name: Deploy to Preview

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  deploy-preview:
    uses: ./.github/workflows/deploy.yml
    with:
      environment: 'Preview'
      is_production: false
